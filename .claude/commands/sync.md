# /sync

Invoke the **`agentgateway-sync`** skill (`.claude/skills/agentgateway-sync/SKILL.md`) and follow its references for inspecting state, classifying risk, rebasing, testing, and opening a PR into `adobe`.
